# First-time Flutter project setup

```
cd frontend
flutter create . --project-name civic_issue_app --org com.civicsystem --platforms=web,windows
flutter pub get
```

Then run the backend and `flutter run -d chrome`.
